const name = 'shaun'

// functions
const greet = () => 'hello'
let resultOne = greet()
console.log(resultOne)

// methods - they are functions but specific to a datatype and where they are defined

let resultTwo = name.toUpperCase()
console.log(resultTwo)
